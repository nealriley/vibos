<?xml version="1.0" encoding="UTF-8"?>
<labwc_config>
  <core>
    <decoration>server</decoration>
    <gap>0</gap>
    <adaptiveSync>no</adaptiveSync>
    <reuseOutputMode>no</reuseOutputMode>
  </core>

  <theme>
    <name>VibeOS</name>
    <cornerRadius>8</cornerRadius>
    <font place="ActiveWindow">
      <name>sans</name>
      <size>10</size>
      <slant>normal</slant>
      <weight>bold</weight>
    </font>
    <font place="InactiveWindow">
      <name>sans</name>
      <size>10</size>
      <slant>normal</slant>
      <weight>normal</weight>
    </font>
  </theme>

  <focus>
    <followMouse>yes</followMouse>
    <followMouseRequiresMovement>yes</followMouseRequiresMovement>
    <raiseOnFocus>no</raiseOnFocus>
  </focus>

  <keyboard>
    <!-- Super+Space: Toggle shell -->
    <keybind key="Super-space">
      <action name="Execute">
        <command>pkill -USR1 -f vibeos-shell || true</command>
      </action>
    </keybind>

    <!-- Super+Enter: Launch terminal -->
    <keybind key="Super-Return">
      <action name="Execute">
        <command>alacritty</command>
      </action>
    </keybind>

    <!-- Super+B: Launch browser -->
    <keybind key="Super-b">
      <action name="Execute">
        <command>firefox-esr</command>
      </action>
    </keybind>

    <!-- Alt+F4: Close window -->
    <keybind key="A-F4">
      <action name="Close"/>
    </keybind>

    <!-- Alt+Tab: Cycle windows -->
    <keybind key="A-Tab">
      <action name="NextWindow"/>
    </keybind>

    <!-- Super+F: Fullscreen toggle -->
    <keybind key="Super-f">
      <action name="ToggleFullscreen"/>
    </keybind>

    <!-- Escape: Emergency show shell -->
    <keybind key="Escape">
      <action name="Execute">
        <command>pkill -USR2 -f vibeos-shell || true</command>
      </action>
    </keybind>
  </keyboard>

  <mouse>
    <default>
      <mousebind button="Left" action="Press">
        <action name="Focus"/>
        <action name="Raise"/>
      </mousebind>
      <mousebind button="Middle" action="Press">
        <action name="Focus"/>
        <action name="Raise"/>
      </mousebind>
    </default>
  </mouse>

  <windowRules>
    <!-- VibeOS Shell: fullscreen, no decorations -->
    <windowRule identifier="vibeos-shell">
      <skipTaskbar>yes</skipTaskbar>
      <skipWindowSwitcher>yes</skipWindowSwitcher>
      <serverDecoration>no</serverDecoration>
    </windowRule>

    <!-- Terminals -->
    <windowRule identifier="Alacritty">
      <serverDecoration>yes</serverDecoration>
    </windowRule>

    <windowRule identifier="foot">
      <serverDecoration>yes</serverDecoration>
    </windowRule>
  </windowRules>
</labwc_config>
